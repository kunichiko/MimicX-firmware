// ===================================================================================
// mimicx_tx.h  (device→host 送信の共通エントリ)
// ===================================================================================
// デバイス→ホストの MIDI/SysEx メッセージ (IDENTIFY_RSP / ACK / TARGET_RX 等) を
// 全トランスポートへ送る。USB-MIDI に加え、2 チップ構成 (MIMICX_I2C) では I2C
// キューにも積む。functions/ 配下のモジュールはホストへ送る際に必ずこれを使うこと
// (usb_midi_send_sysex を直接呼ぶと I2C に乗らず BLE 経由でホストへ届かない)。
// ===================================================================================
#ifndef _MIMICX_TX_H
#define _MIMICX_TX_H

#include <stdint.h>

void mimicx_tx(const uint8_t* data, int len);

#endif
